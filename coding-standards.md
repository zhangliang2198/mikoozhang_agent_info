# Coding Standards

## 用途

记录跨项目通用的编码标准、工程质量要求和实现偏好。项目内已有规范时，以项目规范优先。

## 通用原则

- 优先遵循当前项目已有架构、命名、目录和代码风格。
- 修改范围应贴合任务目标，避免无关重构。
- 对用户可见行为、数据持久化、构建发布链路的改动，需要更严格验证。
- 新增抽象要有明确收益，不能只为了“看起来更规范”。
- 注释应解释复杂原因或边界条件，不写重复代码含义的空泛注释。

## 代码修改检查项

- 是否读过当前实现和调用链。
- 是否兼容已有配置、数据和用户操作流程。
- 是否处理错误路径。
- 是否有必要的日志或提示。
- 是否需要测试、构建或手动验证。
- 是否会影响发布产物。

## 测试策略

```md
## 项目或技术栈

- 推荐测试命令：
- 推荐构建命令：
- 需要手动验证的场景：
- 可接受的已知警告：
- 不可忽略的失败信号：
```

## 语言和框架偏好

后续可按技术栈补充：

- JavaScript / TypeScript
- React / Electron
- Go
- Rust
- Python
- SQL / SQLite / MySQL
- Shell / PowerShell
